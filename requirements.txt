# Core
PySide6>=6.7
sounddevice>=0.4.7
numpy>=1.26,<2.3
faster-whisper>=1.0.3
pystray>=0.19.5
Pillow>=10.0
huggingface_hub>=0.24
scipy>=1.11

# File transcription (Transcribe File tab)
# ffmpeg is required to decode mp3/mp4/etc. Install one of:
#   - System ffmpeg from https://ffmpeg.org (recommended; add to PATH)
#   - imageio-ffmpeg     # bundles a static ffmpeg.exe (auto-discovered)
imageio-ffmpeg>=0.4.9

# Optional: speaker diarization (identifies "who spoke when")
# Requires a free HuggingFace token + accepting the model conditions at:
#   https://huggingface.co/pyannote/speaker-diarization-3.1
# pyannote.audio>=3.1
# torchaudio  # installed alongside PyTorch — see note below

# Optional: Parakeet via NVIDIA NeMo (heavy install)
# nemo_toolkit[asr]>=2.0.0

# PyTorch for RTX 5070 (Blackwell sm_120) — install separately:
# pip install --index-url https://download.pytorch.org/whl/cu128 torch torchaudio
